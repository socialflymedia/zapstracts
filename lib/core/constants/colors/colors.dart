import 'package:flutter/material.dart';

class AppColors {
  // Branding colors
  static const Color primary = Color(0xFF422687);        // Purple lightning
  static const Color secondary = Color(0xFF0A0A0A);      // Dark gradient text/button
  static const Color accent = Color(0xFFE8D7F1)
  ;         // Blue highlights/splashes

  // Background
  static const Color background = Color(0xFFF9F9F9);      // Very light background
  static const Color card = Color(0xFFFFFFFF);

  // Text
  static const Color textPrimary = Color(0xFF1C1C1E);     // Black-ish
  static const Color textSecondary = Color(0xFF8E8E93);   // Gray for skip
  static const Color buttonText = Color(0xFFFFFFFF);      // White for buttons

  // Buttons
  static const Color buttonBackground = Color(0xFF0A0A0A);  // Deep black button
  static const Color buttonShadow = Color(0xFF141414);      // Shadow

  // Others
  static const Color lightPurple = Color(0xFFDAD4F7);      // Optional for hover/effects
  static const Color transparent = Colors.transparent;
}
